process {    
    cache = 'lenient'
    executor = 'slurm'
    executor.queueSize = 20
    stageInMode = 'symlink'
  }

trace {
  enabled = true
  file = "trace.txt"
}

singularity {
       enabled = true
        autoMounts = true
        runOptions = '-B /home/mmunteanu/LINX'
}

